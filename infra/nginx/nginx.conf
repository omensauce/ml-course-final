worker_processes auto;

events {
    worker_connections 1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;
    sendfile      on;
    keepalive_timeout 65;

    # Security headers
    add_header X-Content-Type-Options  nosniff always;
    add_header X-Frame-Options         DENY    always;
    add_header X-XSS-Protection        "1; mode=block" always;
    add_header Referrer-Policy         "strict-origin-when-cross-origin" always;

    # Hide nginx version from error pages and Server header
    server_tokens off;

    upstream inference_api {
        server inference-api:8000;
        keepalive 16;
    }

    upstream svelte_dev {
        server svelte-ui:5173;
        keepalive 8;
    }

    server {
        listen 80;
        server_name _;

        # /api/* → inference-api:8000 (strip /api prefix)
        location /api/ {
            proxy_pass         http://inference_api/;
            proxy_http_version 1.1;
            proxy_set_header   Host              $host;
            proxy_set_header   X-Real-IP         $remote_addr;
            proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header   X-Forwarded-Proto $scheme;
            proxy_set_header   Connection        "";

            # Restrict methods — the API only uses GET and POST
            limit_except GET POST OPTIONS {
                deny all;
            }
        }

        # / → Svelte dev server with WebSocket support for HMR
        location / {
            proxy_pass         http://svelte_dev;
            proxy_http_version 1.1;
            proxy_set_header   Host              $host;
            proxy_set_header   X-Real-IP         $remote_addr;
            proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
            # WebSocket upgrade for Vite HMR
            proxy_set_header   Upgrade           $http_upgrade;
            proxy_set_header   Connection        "upgrade";
        }
    }
}
