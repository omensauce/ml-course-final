# Vite environment variables for the Plant Alarm UI
# Copy to .env and adjust for your environment

# Frontend gateway (auth + history + inference bridge)
VITE_FRONTEND_API_URL=http://localhost:8001

# Mock live sensor API
VITE_SENSOR_API_URL=http://localhost:8002
